# 签到页 + 厨房页 4 项修补 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修 4 件事：① 签到页连续天数算法 ② 签到成功后 ws 推送组钻石变化 ③ 补 `PATCH /api/groups/{id}/name` 改组名端点 ④ 厨房页菜品新增 / 编辑 / 删除三件套接通。

**Architecture:** 后端走 TDD，#1 抽一个 `compute_current_consecutive_days` 纯函数覆盖 3 种情况（今天 / 昨天 / 更早）；#2 新增 `WsGroupDiamondChangeData` + 复用现有 `ConnectionManager.send_to_user` 模式，事务 commit 之后再发；#3 用 `CustomError::BadRequest` 服务端兜底校验 1-10 位。前端**不写测试**（用户明确选择），改 stash.vue 的 3 个函数 + group-name 组件的 `doUpdateUsername` + App.vue 加 ws 监听 + apis/group.ts 加一个 `updateGroupName`。

**Tech Stack:** Rust 2021 / ntex 2.1 / sqlx 0.8 / utoipa 5 / PostgreSQL 16 / Vue 3 + TypeScript 5 (uni-app) / @dcloudio/uni-app

---

## 文件改动总览

| 文件 | 操作 | 任务 |
|---|---|---|
| `src/api/sign_in/routes.rs` | 修改：`compute_current_consecutive_days` 纯函数 + `sign_in_status` 用上 + 单元测试 | T1 |
| `src/api/ws/messages.rs` | 修改：`WsGroupDiamondChangeData` 结构体 + `WsEnvelope::group_diamond_change` 构造方法 | T2 |
| `src/api/sign_in/broadcast.rs` | **新建**：`push_group_diamond_change_notice` 函数 | T3 |
| `src/application/sign_in_service.rs` | 修改：`daily_checkin` commit 之后调 `push_group_diamond_change_notice` | T4 |
| `src/api/groups/routes.rs` | 修改：路由 + `update_group_name` handler + `validate_group_name` 纯函数 + 单元测试 | T5 |
| `wx-store/src/App.vue` | 修改：加 `group_diamond_change` ws 监听 | T6 |
| `wx-store/src/apis/group.ts` | 修改：加 `updateGroupName` | T7 |
| `wx-store/src/components/group-name/index.vue` | 修改：`doUpdateUsername` 接通后端 | T8 |
| `wx-store/src/pages/stash/stash.vue` | 修改：`addNewFood` / `editFood` 跳转；`confirmDelete` 加 try/catch | T9, T10, T11 |
| `src/v3.sql` | **不修改** | — |
| `wx-store/src/pages.json` | **不修改**（newfood 路由已在） | — |

---

## Task 1: 修 `sign_in_status` 连续天数算法（TDD）

**Files:**
- Modify: `src/api/sign_in/routes.rs:181-275`（`sign_in_status` handler）
- Modify: `src/api/sign_in/routes.rs:361-405`（末尾 `#[cfg(test)] mod tests` 追加测试）

**目标:** 抽一个纯函数 `compute_current_consecutive_days(today, last_sign_date, last_consecutive_days)`，覆盖三种情况；`sign_in_status` handler 用上。

### Step 1.1: 在 `routes.rs` 顶部 `pub fn configure` 之后、`// ========== 响应结构 ==========` 之前加 `compute_current_consecutive_days`

在 `src/api/sign_in/routes.rs` 第 33 行（`pub fn configure` 块结束后的空行）后插入：

```rust
/// 算"当前连续签到天数"——前端在没签到今天时也能看到 streak 还在
///
/// 规则(按业务诉求"昨天签了算 1 连续"):
/// - `last_sign_date` 是今天 → 用 `last_consecutive_days`(今天刚签过)
/// - `last_sign_date` 是昨天 → 用 `last_consecutive_days`(streak 还在,等今天签)
/// - `last_sign_date` 比昨天更早 OR 从没签过 → 0(streak 已断)
pub fn compute_current_consecutive_days(
    today: chrono::NaiveDate,
    last_sign_date: Option<chrono::NaiveDate>,
    last_consecutive_days: Option<i32>,
) -> i32 {
    match last_sign_date {
        None => 0,
        Some(d) => {
            let yesterday = today.pred_opt().unwrap();
            if d == today || d == yesterday {
                last_consecutive_days.unwrap_or(0)
            } else {
                0
            }
        }
    }
}
```

### Step 1.2: 在 `sign_in_status` handler 里查最近一条 + 用上函数

替换 `src/api/sign_in/routes.rs` 第 207-216 行那段 `// 获取组成员及其今天的签到状态` 的 SQL 块之后、第 218 行 `let mut member_statuses = Vec::new();` 之前，**追加**：

```rust
    // 额外查一次"当前用户最近一次签到记录" —— 用它算 streak 是否还在
    // 原因: 原来的 LEFT JOIN 只匹配 sr.sign_date = today, 今天没签就拿不到 streak
    let last_sign_row: Option<(chrono::NaiveDate, i32)> = sqlx::query_as(
        "SELECT sign_date, consecutive_days FROM sign_in_records
         WHERE group_id = $1 AND user_id = $2
         ORDER BY sign_date DESC LIMIT 1"
    )
    .bind(group_id)
    .bind(token.user_id)
    .fetch_optional(db)
    .await?;
    let (last_sign_date, last_consecutive_days) = match last_sign_row {
        Some((d, cd)) => (Some(d), Some(cd)),
        None => (None, None),
    };
```

然后找到 `// 当前用户今日签到状态` 那段（约第 243-245 行），把它替换为：

```rust
    // 当前用户今日签到状态
    let today_signed = my_member.map(|m| m.signed).unwrap_or(false);
    // 连续天数用 compute_current_consecutive_days 算 —— 关键修复:
    // 今天没签但昨天签了 → 仍然返回"streak 还在"
    let consecutive_days = compute_current_consecutive_days(
        today,
        last_sign_date,
        last_consecutive_days,
    );
```

### Step 1.3: 加单元测试

在 `src/api/sign_in/routes.rs` 末尾的 `#[cfg(test)] mod tests { ... }` 块里、`}` 之前追加：

```rust
    #[test]
    fn compute_current_consecutive_days_today_signed() {
        let today = chrono::NaiveDate::from_ymd_opt(2026, 6, 25).unwrap();
        // 今天签了, consecutive_days=3 → 保留 3
        assert_eq!(
            compute_current_consecutive_days(today, Some(today), Some(3)),
            3
        );
    }

    #[test]
    fn compute_current_consecutive_days_yesterday_only() {
        let today = chrono::NaiveDate::from_ymd_opt(2026, 6, 25).unwrap();
        let yesterday = chrono::NaiveDate::from_ymd_opt(2026, 6, 24).unwrap();
        // 昨天签了,今天没签,streak 还在 → 保留 1
        assert_eq!(
            compute_current_consecutive_days(today, Some(yesterday), Some(1)),
            1
        );
    }

    #[test]
    fn compute_current_consecutive_days_broken() {
        let today = chrono::NaiveDate::from_ymd_opt(2026, 6, 25).unwrap();
        let three_days_ago = chrono::NaiveDate::from_ymd_opt(2026, 6, 22).unwrap();
        // 3 天前签的,streak 断了 → 0
        assert_eq!(
            compute_current_consecutive_days(today, Some(three_days_ago), Some(5)),
            0
        );
    }

    #[test]
    fn compute_current_consecutive_days_never_signed() {
        let today = chrono::NaiveDate::from_ymd_opt(2026, 6, 25).unwrap();
        // 从没签过 → 0
        assert_eq!(compute_current_consecutive_days(today, None, None), 0);
    }
```

### Step 1.4: 跑测试

```bash
cd /home/peter/project/may_store && cargo test --lib api::sign_in::routes::tests
```

Expected: 4 个新测试全部 PASS（其他已有的 sign_in_status_response_* 测试也 PASS）。

### Step 1.5: 跑全量 cargo build 确认没破坏其它东西

```bash
cd /home/peter/project/may_store && cargo build 2>&1 | tail -20
```

Expected: `Finished` 字样,无 error。

### Step 1.6: Commit

```bash
git add src/api/sign_in/routes.rs
git commit -m "fix(签到): sign_in_status 的连续天数算法支持 streak 还在场景

compute_current_consecutive_days 纯函数覆盖 4 种情况:
- 今天签了
- 昨天签了今天没签(streak 还在)
- 更早(streak 断)
- 从没签过

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: 加 `WsGroupDiamondChangeData` 类型

**Files:**
- Modify: `src/api/ws/messages.rs`（在 `WsGroupMemberChangeData` 之后追加新结构体和构造方法）

**目标:** 加一种 ws 消息类型,字段含 groupId / userId / diamond / exp / level / consecutiveDays / reason。

### Step 2.1: 在 `WsGroupMemberChangeData` 之后（第 99 行后）追加新结构体

在 `src/api/ws/messages.rs` 第 99 行（`pub struct WsGroupMemberChangeData { ... }` 的结束 `}` 之后）的空行处插入：

```rust
/// 组钻石/经验变化数据
///
/// 触发场景:
/// - 签到成功(本次新增)
/// - 后续其它会动组钻石的业务(留扩展位)
///
/// reason 取值:
/// - "sign_in": 签到
/// - "full_team_bonus": 全组满签奖励
/// - 后续可加 "order_reward" 等
#[allow(dead_code)]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct WsGroupDiamondChangeData {
    pub group_id: i64,
    /// 触发本次钻石变化的用户(签到时是签到者)
    pub user_id: i64,
    pub diamond: i32,
    pub exp: i32,
    pub level: i32,
    pub consecutive_days: i32,
    pub reason: String,
}
```

### Step 2.2: 在 `WsEnvelope` impl 块内（约 119-175 行）`group_member_change` 构造方法之后追加

在 `src/api/ws/messages.rs` 中找到 `pub fn group_member_change(change: &WsGroupMemberChangeData) -> Self` 函数（约 158-163 行），在其结束 `}` 之后的空行处插入：

```rust
    /// 创建组钻石变化消息
    pub fn group_diamond_change(change: &WsGroupDiamondChangeData) -> Self {
        Self {
            msg_type: "group_diamond_change".to_string(),
            data: serde_json::json!(change),
        }
    }
```

### Step 2.3: 跑 cargo build 确认

```bash
cd /home/peter/project/may_store && cargo build 2>&1 | tail -20
```

Expected: `Finished`,无 error。

### Step 2.4: Commit

```bash
git add src/api/ws/messages.rs
git commit -m "feat(ws): 加 group_diamond_change 消息类型

字段: groupId / userId / diamond / exp / level / consecutiveDays / reason
构造方法: WsEnvelope::group_diamond_change
reason 暂定取值: 'sign_in' / 'full_team_bonus'

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: 新建 `src/api/sign_in/broadcast.rs` 推送函数

**Files:**
- Create: `src/api/sign_in/broadcast.rs`
- Modify: `src/api/sign_in/mod.rs`（加 `pub mod broadcast;`）

**目标:** 抽一个 `push_group_diamond_change_notice` 函数,风格跟 `push_group_member_change_notice` 保持一致。

### Step 3.1: 新建 `src/api/sign_in/broadcast.rs`

文件内容:

```rust
// API - 签到相关 ws 广播
// 签到成功 / 全组满签后, 给组里所有 ACTIVE 成员推 group_diamond_change

use crate::api::ws::get_connection_manager;
use crate::api::ws::messages::{
    WsEnvelope, WsGroupDiamondChangeData,
};
use sqlx::Row;
use sqlx::PgPool;

/// 签到成功后, 推"组钻石变了"给全组在线成员
///
/// 设计: 跟 push_group_member_change_notice 一样, 按 group_id 反查所有 ACTIVE
/// 成员 -> 逐个推。target 不在线静默丢弃(业务侧说"暂时不管离线")。
///
/// 调用方: src/application/sign_in_service.rs::daily_checkin
/// 时机: 事务 commit 之后, HTTP 200 返回之前
pub async fn push_group_diamond_change_notice(
    db: &PgPool,
    group_id: i64,
    user_id: i64,
    reason: &str,
    consecutive_days: i32,
) {
    // 拿 group 最新信息
    let group_row: Option<(i32, i32, i32)> = match sqlx::query_as(
        "SELECT diamond, exp, level FROM association_groups WHERE group_id = $1",
    )
    .bind(group_id)
    .fetch_optional(db)
    .await
    {
        Ok(r) => r,
        Err(e) => {
            log::error!(
                "[push_group_diamond_change] 查 group_id={} 失败: {:?}",
                group_id, e
            );
            return;
        }
    };

    let (diamond, exp, level) = match group_row {
        Some(v) => v,
        None => {
            log::error!(
                "[push_group_diamond_change] group_id={} 不存在",
                group_id
            );
            return;
        }
    };

    let payload = WsGroupDiamondChangeData {
        group_id,
        user_id,
        diamond,
        exp,
        level,
        consecutive_days,
        reason: reason.to_string(),
    };

    let envelope = WsEnvelope::group_diamond_change(&payload);
    let Ok(json) = serde_json::to_string(&envelope) else {
        log::error!("组钻石变化通知序列化失败: {:?}", payload);
        return;
    };

    // 按 group_id 反查所有 ACTIVE 成员 -> 全体推送
    let target_ids: Vec<i64> = match sqlx::query_scalar(
        "SELECT user_id FROM association_group_members
         WHERE group_id = $1 AND member_status = 'ACTIVE'",
    )
    .bind(group_id)
    .fetch_all(db)
    .await
    {
        Ok(v) => v,
        Err(e) => {
            log::error!(
                "[push_group_diamond_change] 查 group_id={} 成员失败: {:?}",
                group_id, e
            );
            Vec::new()
        }
    };

    let manager = get_connection_manager();
    for target_id in target_ids {
        let sent = manager.send_to_user(target_id, &json).await;
        if !sent {
            log::debug!(
                "组钻石变化通知未送达(用户 {} 不在线): group_id={} reason={}",
                target_id, group_id, reason
            );
        }
    }
}
```

### Step 3.2: 在 `src/api/sign_in/mod.rs` 加 `pub mod broadcast;`

打开 `src/api/sign_in/mod.rs`,在文件已有 module 声明附近（按 `pub mod routes;` 的风格）追加一行:

```rust
pub mod broadcast;
```

### Step 3.3: 跑 cargo build

```bash
cd /home/peter/project/may_store && cargo build 2>&1 | tail -20
```

Expected: `Finished`,无 error。提示 `Row` trait 未使用 -> 把 `use sqlx::Row;` 删掉（这个文件其实没用 `Row`）。

### Step 3.4: Commit

```bash
git add src/api/sign_in/broadcast.rs src/api/sign_in/mod.rs
git commit -m "feat(签到): 加 push_group_diamond_change_notice 广播函数

按 group_id 反查所有 ACTIVE 成员 -> 逐个推 group_diamond_change
target 不在线静默丢弃

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: 在 `daily_checkin` 调推送

**Files:**
- Modify: `src/application/sign_in_service.rs`（在 `daily_checkin` 末尾、return Ok 之前追加调用）

**目标:** commit 之后、`Ok(DailyCheckinOut { ... })` 之前调一次 `push_group_diamond_change_notice`。

### Step 4.1: 在文件顶部 `use` 段加 import

打开 `src/application/sign_in_service.rs`,在已有 `use` 段（按现有风格）追加一行:

```rust
use crate::api::sign_in::broadcast::push_group_diamond_change_notice;
```

### Step 4.2: 在 `daily_checkin` 末尾追加调用

找到 `tx.commit().await?;` 这一行（约 181 行）之后、`// 6. 拿最终组钻石用于返回` 之前（也就是 181 行的下一个空行后、183 行之前）插入:

```rust
        // 5.5 推 group_diamond_change 通知给全组 —— commit 之后再发,
        //   即使推送失败也不影响签到结果; 失败/离线静默丢弃由函数内部处理
        let reason = if full_team_bonus_amt > 0 {
            "full_team_bonus"
        } else {
            "sign_in"
        };
        push_group_diamond_change_notice(
            db,
            group_id,
            user_id,
            reason,
            consecutive_days,
        )
        .await;
```

### Step 4.3: 跑 cargo build

```bash
cd /home/peter/project/may_store && cargo build 2>&1 | tail -20
```

Expected: `Finished`,无 error。

### Step 4.4: Commit

```bash
git add src/application/sign_in_service.rs
git commit -m "feat(签到): daily_checkin commit 后推 group_diamond_change

按 reason 区分: 'sign_in' / 'full_team_bonus'
失败/离线静默丢弃, 不影响签到返回 200

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: 加 `PATCH /api/groups/{id}/name` 端点（TDD）

**Files:**
- Modify: `src/api/groups/routes.rs`（加路由 + `validate_group_name` 纯函数 + `update_group_name` handler + 单元测试）
- Modify: `src/api/groups/routes.rs`（在 `configure` 函数里加路由注册）

**目标:** 服务端兜底校验组名 1-10 位中英文/数字/下划线,只允许组内 ACTIVE 成员改。

### Step 5.1: 加 `validate_group_name` 纯函数

在 `src/api/groups/routes.rs` 文件里、找一个合适的位置（建议放在 `push_group_member_change_notice` 函数之后,`// ============== Group Point Config HTTP 路由 ==============` 注释之前），追加:

```rust
/// 校验组名: 1-10 位中英文 / 数字 / 下划线
///
/// 跟前端 group-name 组件的正则保持一致:
/// `/^[一-龥a-zA-Z0-9_]{1,10}$/`
pub fn validate_group_name(name: &str) -> Result<(), CustomError> {
    if name.is_empty() {
        return Err(CustomError::BadRequest("组名不能为空".into()));
    }
    if name.chars().count() > 10 {
        return Err(CustomError::BadRequest("组名不能超过 10 个字符".into()));
    }
    let mut ok = true;
    for c in name.chars() {
        let is_chinese = '\u{4e00}' <= c && c <= '\u{9fa5}';
        let is_ascii_alnum = c.is_ascii_alphanumeric();
        let is_underscore = c == '_';
        if !(is_chinese || is_ascii_alnum || is_underscore) {
            ok = false;
            break;
        }
    }
    if !ok {
        return Err(CustomError::BadRequest(
            "组名只能包含中英文、数字、下划线".into(),
        ));
    }
    Ok(())
}
```

### Step 5.2: 加 `update_group_name` handler

在 `validate_group_name` 函数之后追加:

```rust
/// 更新组名响应体
#[derive(Debug, Serialize, ToSchema)]
#[serde(rename_all = "camelCase")]
pub struct UpdateGroupNameResponse {
    pub group_id: i64,
    pub group_name: String,
}

/// 更新组名
/// PATCH /api/groups/{group_id}/name
///
/// 鉴权: 当前用户必须是该 group 的 ACTIVE 成员(任意一方都能改)
/// 业务上不广播 ws(改组名是低频动作, 对方下次进厨房页下拉刷新即可)
/// 返回: 仅 { group_id, group_name },前端拿到后再单独调 get_group 拉完整信息
#[utoipa::path(
    patch,
    path = "/api/groups/{group_id}/name",
    tag = "双人组",
    params(("group_id" = i64, Path, description = "组ID")),
    request_body = UpdateGroupNameRequest,
    responses(
        (status = 200, description = "更新成功", body = UpdateGroupNameResponse),
        (status = 400, description = "组名格式错误"),
        (status = 401, description = "未登录"),
        (status = 403, description = "非组成员"),
        (status = 404, description = "组不存在")
    ),
    security(("bearer_auth" = []))
)]
pub async fn update_group_name(
    state: State<Arc<AppState>>,
    token: UserToken,
    path: Path<i64>,
    body: Json<UpdateGroupNameRequest>,
) -> Result<impl Responder, CustomError> {
    let group_id = path.into_inner();
    let input = body.into_inner();
    let db = &state.db_pool;

    // 校验组名
    validate_group_name(&input.name)?;

    // 鉴权: 必须是该组 ACTIVE 成员
    let is_member: Option<(i64,)> = sqlx::query_as(
        "SELECT user_id FROM association_group_members
         WHERE group_id = $1 AND user_id = $2 AND member_status = 'ACTIVE'"
    )
    .bind(group_id)
    .bind(token.user_id)
    .fetch_optional(db)
    .await?;
    if is_member.is_none() {
        return Err(CustomError::Forbidden("不是该组成员".into()));
    }

    // 更新 group_name
    let affected: u64 = sqlx::query(
        "UPDATE association_groups SET group_name = $1, updated_at = NOW()
         WHERE group_id = $2",
    )
    .bind(&input.name)
    .bind(group_id)
    .execute(db)
    .await?
    .rows_affected();
    if affected == 0 {
        return Err(CustomError::NotFound("组不存在".into()));
    }

    Ok(ApiResponse::success(UpdateGroupNameResponse {
        group_id,
        group_name: input.name,
    }))
}
```

### Step 5.3: 加 `UpdateGroupNameRequest` 结构体

在 `validate_group_name` 函数之前（同一个作用域,文件内合适位置）追加:

```rust
/// 改组名请求体
#[derive(Debug, Deserialize, ToSchema)]
#[serde(rename_all = "camelCase")]
pub struct UpdateGroupNameRequest {
    pub name: String,
}
```

### Step 5.4: 在 `configure` 函数里注册路由

在 `src/api/groups/routes.rs` 找到现有的 `update_group_point_config` 路由注册（约 110-113 行那个 `web::resource("/api/groups/{group_id}/point-config")`），在其后追加:

```rust
    cfg.service(
        web::resource("/api/groups/{group_id}/name")
            .guard(group_id_is_numeric())
            .route(web::patch().to(update_group_name)),
    );
```

### Step 5.5: 加单元测试

在 `src/api/groups/routes.rs` 文件末尾的 `#[cfg(test)] mod tests` 块（如有则追加;如无则新建一个）内追加:

```rust
    #[test]
    fn validate_group_name_ok_chinese() {
        assert!(validate_group_name("开心厨房").is_ok());
    }

    #[test]
    fn validate_group_name_ok_english_underscore() {
        assert!(validate_group_name("My_Kitchen1").is_ok());
    }

    #[test]
    fn validate_group_name_ok_max_len() {
        assert!(validate_group_name("一二三四五六七八九十").is_ok());
    }

    #[test]
    fn validate_group_name_empty() {
        assert!(validate_group_name("").is_err());
    }

    #[test]
    fn validate_group_name_too_long() {
        assert!(validate_group_name("一二三四五六七八九十X").is_err());
    }

    #[test]
    fn validate_group_name_special_char() {
        assert!(validate_group_name("hi@you").is_err());
    }

    #[test]
    fn validate_group_name_space() {
        assert!(validate_group_name("hi you").is_err());
    }
```

### Step 5.6: 跑测试

```bash
cd /home/peter/project/may_store && cargo test --lib api::groups::routes::tests 2>&1 | tail -20
```

Expected: 7 个新测试全部 PASS。

### Step 5.7: 跑全量 build

```bash
cd /home/peter/project/may_store && cargo build 2>&1 | tail -20
```

Expected: `Finished`,无 error。

### Step 5.8: Commit

```bash
git add src/api/groups/routes.rs
git commit -m "feat(组): 加 PATCH /api/groups/{id}/name 改组名端点

- validate_group_name 纯函数: 1-10 位中英文/数字/下划线
- 鉴权: 必须是组内 ACTIVE 成员
- 不广播 ws(低频动作, 下次进入下拉刷新即可)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: 前端 App.vue 加 `group_diamond_change` ws 监听

**Files:**
- Modify: `wx-store/src/App.vue:162-174`（在现有 `group_member_change` 监听之后追加）

**目标:** 跟现有 `group_member_change` 走完全一样的路径,差异只在消息类型。

### Step 6.1: 在 App.vue 找到现有 `socketManager.on("group_member_change", ...)` 块

在第 162-174 行那段 handler 之后（即第 174 行 `}` 之后的空行处）插入:

```js
  // 签到 / 满签等场景下, 组的钻石/经验/等级会变,
  // 听到消息就重新拉一次 groupInfo, 让厨房页等地方数字自动跳
  socketManager.on("group_diamond_change", (data: any) => {
    const gid = data?.groupId;
    if (!gid) return;
    groupStore.fetchGroupInfo(gid).catch((e: any) => {
      console.warn("[ws] fetchGroupInfo (diamond_change) failed", e);
    });
  });
```

### Step 6.2: 跑前端 lint（如果项目有）

```bash
cd /home/peter/project/wx-store && pnpm run lint 2>&1 | tail -20
```

Expected: 无 error（warning 可以接受）。如果项目没配 lint 脚本,跳过这步,继续 Step 6.3。

### Step 6.3: Commit

```bash
cd /home/peter/project/may_store
git add ../wx-store/src/App.vue
git commit -m "feat(ws): App.vue 加 group_diamond_change 监听

跟 group_member_change 走完全一样的路径: 拿到 groupId -> 重新 fetchGroupInfo

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 7: 前端 apis/group.ts 加 `updateGroupName`

**Files:**
- Modify: `wx-store/src/apis/group.ts`（追加 `updateGroupName` 函数）

**目标:** 走 `patchRequest` 打 `PATCH /api/groups/{id}/name`。

### Step 7.1: 在 `wx-store/src/apis/group.ts` 文件末尾追加

```ts
/**
 * 修改组名
PATCH /api/groups/{group_id}/name
 */
export function updateGroupName(
  pathParams: { group_id: number },
  body: { name: string }
): Promise<any> {
  return patchRequest(
    `/api/groups/${pathParams.group_id}/name`,
    true,
    body
  )
}
```

### Step 7.2: Commit

```bash
cd /home/peter/project/may_store
git add ../wx-store/src/apis/group.ts
git commit -m "feat(组): apis/group.ts 加 updateGroupName

PATCH /api/groups/{id}/name, 走 patchRequest

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 8: 接通 group-name 组件的 doUpdateUsername

**Files:**
- Modify: `wx-store/src/components/group-name/index.vue:90-91`（import 行加 `updateGroupName`）
- Modify: `wx-store/src/components/group-name/index.vue:190-195`（`doUpdateUsername` 函数体）

**目标:** 去掉 "暂未接入" toast,改成调 `updateGroupName`,成功/失败分别处理。

### Step 8.1: 在 import 段加 `updateGroupName`

在 `wx-store/src/components/group-name/index.vue` 第 91 行:

```ts
import { getGroup, getGroupMembers } from '@/apis/group'
```

改为:

```ts
import { getGroup, getGroupMembers, updateGroupName } from '@/apis/group'
```

### Step 8.2: 把 `doUpdateUsername` 改成异步 + 调接口

找到第 190-195 行的 `doUpdateUsername` 函数:

```ts
function doUpdateUsername() {
  const group_id: number = groupInfo.value.groupId
  // TODO: 新 API 中 updateGroup 已被移除, 暂未提供 groupName 字段更新接口
  uni.$toast('团队名称更新接口暂未接入新 API')
  showNameModal.value = false
}
```

整段替换为:

```ts
async function doUpdateUsername() {
  const group_id: number = groupInfo.value.groupId
  try {
    await updateGroupName({ group_id }, { name: newGroupName.value })
    uni.$toast('修改成功', { icon: 'success' })
    showNameModal.value = false
    // 重新拉一次, 当前页组名立刻刷新
    await fetchGroupInfo(group_id)
  } catch (e: any) {
    // 后端兜底校验 / 网络错 / 没权限 都走这里
    // 弹窗不关, 让用户重试或修改
    uni.$toast(e?.errMsg || e?.message || '修改失败')
  }
}
```

### Step 8.3: Commit

```bash
cd /home/peter/project/may_store
git add ../wx-store/src/components/group-name/index.vue
git commit -m "feat(组): group-name 组件接通 updateGroupName

成功 -> fetchGroupInfo 重新拉组信息
失败 -> 弹错误 toast, 弹窗不关

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 9: 接通 addNewFood（"+ 号"）

**Files:**
- Modify: `wx-store/src/pages/stash/stash.vue:468-479`（`addNewFood` 函数体）

**目标:** 去掉 "添加菜品暂未开放" toast,改成 navigateTo。

### Step 9.1: 替换 `addNewFood` 函数体

找到 `wx-store/src/pages/stash/stash.vue` 第 468-479 行的 `addNewFood` 函数:

```js
async function addNewFood() {
  await store.silentLogin()
  if (store.userInfo?.role === 'Ordering') {
    return uni.$toast('身份已变更，正在刷新界面...')
  }
  if (!store.userInfo?.groupId) return uni.$toast('请先绑定用户关系')
  if (leftMenu.value.filter(i => i.id !== 0).length < 1) {
    return uni.$toast('请先添加标签')
  }
  // TODO: pages/newfood 已从 pages.json 中移除, 等待后续重新接入
  uni.$toast('添加菜品暂未开放')
}
```

整段替换为:

```js
async function addNewFood() {
  await store.silentLogin()
  if (store.userInfo?.role === 'Ordering') {
    return uni.$toast('身份已变更，正在刷新界面...')
  }
  if (!store.userInfo?.groupId) return uni.$toast('请先绑定用户关系')
  if (leftMenu.value.filter(i => i.id !== 0).length < 1) {
    return uni.$toast('请先添加标签')
  }
  uni.navigateTo({ url: '/pages/newfood/newfood' })
}
```

### Step 9.2: Commit

```bash
cd /home/peter/project/may_store
git add ../wx-store/src/pages/stash/stash.vue
git commit -m "feat(菜品): stash 厨房页 + 号接通新增菜品

去掉 '添加菜品暂未开放' toast, 改成 navigateTo /pages/newfood/newfood

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 10: 接通 editFood（笔图标）

**Files:**
- Modify: `wx-store/src/pages/stash/stash.vue:398-404`（`editFood` 函数体）

**目标:** 去掉 "菜品编辑暂未开放" toast,改成 navigateTo,保留 setStorageSync。

### Step 10.1: 替换 `editFood` 函数体

找到 `wx-store/src/pages/stash/stash.vue` 第 398-404 行的 `editFood` 函数:

```js
function editFood(f: FoodItem) {
  if (visitMode.value) return
  const editInfo = { ...f }
  uni.setStorageSync('editFood', JSON.stringify(editInfo))
  // TODO: pages/newfood 已从 pages.json 中移除, 等待后续重新接入
  uni.$toast('菜品编辑暂未开放')
}
```

整段替换为:

```js
function editFood(f: FoodItem) {
  if (visitMode.value) return
  const editInfo = { ...f }
  uni.setStorageSync('editFood', JSON.stringify(editInfo))
  // newfood 页 onLoad 会读 'editFood' 这个 key 来预填表单
  uni.navigateTo({ url: '/pages/newfood/newfood' })
}
```

### Step 10.2: Commit

```bash
cd /home/peter/project/may_store
git add ../wx-store/src/pages/stash/stash.vue
git commit -m "feat(菜品): stash 厨房页笔图标接通编辑

去掉 '菜品编辑暂未开放' toast
setStorageSync('editFood') 保留(newfood 页读这个 key 预填表单)
加 navigateTo /pages/newfood/newfood

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 11: 加固 confirmDelete（垃圾桶）

**Files:**
- Modify: `wx-store/src/pages/stash/stash.vue:412-422`（`confirmDelete` 函数体）

**目标:** 加 try/catch,失败时弹错误 toast。

### Step 11.1: 替换 `confirmDelete` 函数体

找到 `wx-store/src/pages/stash/stash.vue` 第 412-422 行的 `confirmDelete` 函数:

```js
function confirmDelete() {
  if (!foodToDelete.value) return
  deletePopup.value?.close()
  apiDeleteFood({
    group_id: (store.userInfo?.groupId as number) || ((foodToDelete.value as any)?.groupId),
    food_id: foodToDelete.value.foodId
  }).then(() => {
    uni.$toast('删除成功')
    fetchFoods()
  })
}
```

整段替换为:

```js
async function confirmDelete() {
  if (!foodToDelete.value) return
  const target = foodToDelete.value
  deletePopup.value?.close()
  try {
    await apiDeleteFood({
      group_id: (store.userInfo?.groupId as number) || ((target as any)?.groupId),
      food_id: target.foodId
    })
    uni.$toast('删除成功')
    fetchFoods()
  } catch (e: any) {
    // 网络错 / 没权限 / 已被其他端删了 等
    // 弹窗已关, 菜还在(因为 fetchFoods 没跑)
    uni.$toast(e?.errMsg || e?.message || '删除失败')
  }
}
```

### Step 11.2: Commit

```bash
cd /home/peter/project/may_store
git add ../wx-store/src/pages/stash/stash.vue
git commit -m "feat(菜品): stash 厨房页删除加 try/catch + 错误 toast

之前是裸 .then, 删失败用户无感知
现在: 失败 -> 弹错误 toast, 菜还在(没跑 fetchFoods)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 验收（按 spec 第 7 节跑一遍）

后端:
```bash
cd /home/peter/project/may_store
cargo build
cargo test --lib api::sign_in::routes::tests::compute_current_consecutive_days
cargo test --lib api::groups::routes::tests::validate_group_name
```

前端手动验证（4 个验收场景按 spec 第 7 节）:
1. 签到页进度条：用账号甲,昨天签到 → 今天进签到页 → 第 1 格亮；3 天没签 → 7 格全灰
2. 实时推送：账号甲开厨房页 + 账号乙开厨房页 → 乙签到 → 甲钻石数字几秒内跳
3. 改组名：点改组名图标 → 改 → 保存 → 弹"修改成功" → 当前页组名变新名字
4. 菜品三件套：+ 号 → 新增；笔 → 编辑（旧值预填）；垃圾桶 → 删除（带确认 + 错误提示）

如果 4 个场景都通过,任务完成。
